/**
 * C program to insert an element in array at specified position
 */
 #include<stdio.h>
 #define MAX_VALUE 1000
 
 int main()
 {
 	int arr[MAX_VALUE],i,size,temp[MAX_VALUE];
 	printf("Enter the size of Array: ");
 	scanf("%d",&size);
 	
 	printf("Enter the elements in Array--\n");
	for(i=0;i<size;i++)
	{
		
	}
 }
